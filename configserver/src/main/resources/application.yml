server:
  port: 8071

spring:
  application:
    name: "configserver"
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/AshGaur/flashstack-centralized-config.git"
          default-label : "main"
          timeout: 5
          clone-on-start: true
          force-pull: true

encrypt:
  key: "411656E7F74154C4B9158DCB53443"

management:
  endpoints:
    web:
      exposure:
        include: "*"